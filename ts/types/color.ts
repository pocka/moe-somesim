export type Hsv = [number, number, number]

export type Rgb = [number, number, number]

export type ColorIndex = [number, number, number]

export type ColorPercentage = [number, number, number]

export const Red = 0
export const Green = 1
export const Blue = 2
